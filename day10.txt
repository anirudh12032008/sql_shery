USE CM;
SET sql_safe_updates = 0;
SELECT ROUND(AVG(CREDITLIMIT),2) FROM CUSTOMERS;
SELECT AMOUNT,FLOOR(AMOUNT) AS FLOOR_AMOUNT, CEIL(AMOUNT) AS CEIL_AMOUNT FROM PAYMENTS;
SELECT TRUNCATE(AVG(CREDITLIMIT),2) FROM CUSTOMERS;
DROP TABLE COMMENTS;
CREATE TABLE COMMENTS(COMMENT_ID INT,POST_ID INT, USER_ID INT,COMMENT_MSG VARCHAR(55), COMMENT_TIME DATETIME ON UPDATE NOW());
INSERT INTO COMMENTS VALUES(1,1,1,'SDGSDG',DEFAULT);
INSERT INTO COMMENTS VALUES(2,2,2,'OP',NOW());
UPDATE COMMENTS SET COMMENT_ID = 3  WHERE COMMENT_ID = 1;
SELECT * FROM COMMENTS;
